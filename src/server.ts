import { app } from './app';

app.listen(3333, () => {
    console.log(app);
});

/**
 * to run server un dev execution follow below
 * yarn start --> to production mode
 * yarn dev --> to development mode
 */